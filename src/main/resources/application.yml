server:
  port: ${PORT:8080}

spring:
  application:
    name: distribuidora-back
  datasource:
    url: ${DATABASE_URL:jdbc:postgresql://localhost:5432/distribuidora}
    username: ${DATABASE_USERNAME:postgres}
    password: ${DATABASE_PASSWORD:postgres}
    hikari:
      maximum-pool-size: 5
  jpa:
    hibernate:
      ddl-auto: update
    open-in-view: false
    properties:
      hibernate:
        jdbc:
          time_zone: UTC
management:
  endpoints:
    web:
      exposure:
        include: health,info
  endpoint:
    health:
      probes:
        enabled: true

app:
  cors:
    allowed-origins: ${CORS_ALLOWED_ORIGINS:*}
  admin:
    username: ${ADMIN_USERNAME:admin}
    password: ${ADMIN_PASSWORD:admin123}
    token-secret: ${ADMIN_TOKEN_SECRET:distribuidora-dev-secret-change-me}
    token-ttl-seconds: ${ADMIN_TOKEN_TTL_SECONDS:86400}
